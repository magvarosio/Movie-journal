

import React from 'react'

const Watchlist = (props) => {
  return (
    <>
      {props.movies.map((movie, index) => {

      })}

      < div > Watchlist</div>
    </>
  )
}

export default Watchlist